# Read the Docs build configuration (must live at the repository root).
# Read the Docs reads this on every push and rebuilds the site automatically.
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

mkdocs:
  configuration: atlaslens-docs/mkdocs.yml

python:
  install:
    - requirements: atlaslens-docs/requirements.txt
